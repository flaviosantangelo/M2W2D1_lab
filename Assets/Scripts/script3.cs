using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class script3 : MonoBehaviour
{
    public int num = 1; 
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Script di prova"); //
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
